name: Public Portal Tests

on:
  pull_request:
    branches:
      - staging
      - master
    paths:
      - 'ui/public-portal/**'
      - '.github/workflows/tc-public-portal-test.yml'
      - '.nvmrc'

  push:
    branches:
      - '**'
      - '!staging'
      - '!master'
    paths:
      - 'ui/public-portal/**'
      - '.github/workflows/tc-public-portal-test.yml'
      - '.nvmrc'

permissions:
  contents: read

jobs:
  public-portal-tests:
    name: Run Public Portal Angular Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ui/public-portal

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: ui/public-portal/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Angular tests
        run: npm run test -- --watch=false --browsers=ChromeHeadless
        env:
          CI: true

      - name: Archive test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: public-portal-test-results
          path: |
            ui/public-portal/coverage
            ui/public-portal/dist/test-results
          if-no-files-found: ignore